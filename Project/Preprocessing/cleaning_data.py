# It contain a function called `preprocess()` that will take a new house's data as input and return those data preprocessed as output.
# If your data doesn't contain the required information, you should return an error to the user.


